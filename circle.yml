version: 2.0

jobs:
  deploy:
    machine:
      enabled: true
    steps:
      - run:
          name: Deploy
          command: bash ./tools/deploy.sh # run a script to set up Heroku
workflows:
  version: 2
  build-and-deploy:
    jobs:
      - deploy:
          filters:
            branches:
              only:  master
